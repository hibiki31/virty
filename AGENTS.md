# Vrity 開発ガイドライン

## 必須手順

### 作業開始前

1. `docs/README.md`を読み、作業に必要な文書だけを読む。
2. タスク開始時はGit worktreeがCodex機能により作成されている、編集前に`codex/<task-name>`などの
   作業ブランチを作成する。detached HEADや`main`では作業しない。
3. `git status`で既存変更を確認し、利用者の変更を保持する。
4. 仕様変更を伴う場合は、実装前に該当docsへ方針を反映する。

### 作業開始後

1. 実装の詳細はコードを正本とし、docsへコード・API・DB列を重複記載しない。
2. DB変更にはAlembic migrationを追加する。
3. API契約変更時はバックエンドschemaとfrontendの型を同時に更新する。
4. 開発・試験はDocker Composeで行い、タスク固有のComposeプロジェクト名で
   コンテナ、ネットワーク、volumeを他の並列作業から分離する。
6. 修正内容に応じて、変更した要件・設計・手順・進捗を必ずdocsへ反映する。
7. 影響範囲に応じたテストを実行する。
9. 変更を理解可能な単位で必ず`git commit`する。
10. `main`へマージする直前に、他の並行作業でマージされた変更を確認する。
    最新`main`を作業ブランチへ取り込み、競合があれば作業ブランチ上で双方の意図を
    保持して解消し、影響範囲を再テストする。

## 留意事項

- `./docs`と`./mkdocs`の役割分担
  - `./docs`はcodex等、AIエージェントと開発者が更新・参照する。
  - `./mkdocs`はAIエージェントと開発者が更新し、利用者が参照する。